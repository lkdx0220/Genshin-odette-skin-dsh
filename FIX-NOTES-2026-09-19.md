# dsh-odette-skin 0.1.0 修复记录（2026-09-19）

修复对象：`C:\Users\24701\Desktop\skin\odette-skin`
修复环境：DSH Desktop 2.0.10（上游 0.1.5-rc.2）
原文件备份：
- `C:\Users\24701\Desktop\DeepSeek Harness\.dl-updates\backup\odette-skin-package.json-before-inject-fix\package.json`
- `C:\Users\24701\Desktop\DeepSeek Harness\.dl-updates\backup\odette-skin-cordis.patch.yml-before-id-fix\cordis.patch.yml`

两个 bug 都在**包根配置文件**里，不在 `src/` 源码中，所以修完**不需要重新构建**（`npm run build` 只重打包 lib/，不覆盖根配置）。

---

## Bug 1：dsh.client.inject 引用了 0.1.5 已移除的包

**文件**：`package.json` → `dsh.client.inject`

**修改前**：
```json
"inject": [
    "@deepseek-ai/dsh-client-runtime",
    "@deepseek-ai/dsh-client-ui-slots"
]
```

**修改后**：
```json
"inject": [
    "@deepseek-ai/dsh-client-ui-slots"
]
```

**原因**：`@deepseek-ai/dsh-client-runtime` 在 DSH 0.1.5-rc.2 已不再发布（它的 client-bootstrap 职责并入 `@deepseek-ai/dsh-client-store`）。client-modules 会按 `inject` 计算模块加载顺序，引用一个不存在的包会导致排序/加载异常。

**依据**：`dsh-session-manager` 0.4.11 的 CHANGELOG 记录了完全相同的改动：
> chore(client): drop `@deepseek-ai/dsh-client-runtime` from `dsh.client.inject`. The package is no longer shipped by DSH 0.1.5-rc.2 / 0.1.2-alpha or newer (its client-bootstrap role was folded into `@deepseek-ai/dsh-client-store`).

---

## Bug 2：cordis.patch.yml 的 entry id 与 client 注册 id 不一致

**文件**：`cordis.patch.yml`

**修改前**：
```yaml
- insert:
    - id: odette-skin
      name: 'dsh-odette-skin'
```

**修改后**：
```yaml
- insert:
    - id: dsh-odette-skin
      name: 'dsh-odette-skin'
```

**原因**：`lib/client.js` 第 2 行注册的 id 是 `dsh-odette-skin`：
```js
window.__ModuleLoader__.load({
	id: "dsh-odette-skin",
	factory: (require) => { ... }
```
而 `cordis.patch.yml` 自己的注释就要求三者必须一致：
```
# NOTE: id/name must match the package name and the client.js registration id
# (__ModuleLoader__.load id) exactly - all three are "dsh-odette-skin".
```
entry id 写成 `odette-skin` 时，loader 条目 id 与客户端注册 id 对不上，皮肤会**加载后不生效**——**不报错，只是没效果**（这类静默失效最难查）。

**修复后三处一致性**：

| 位置 | 值 |
|---|---|
| 包名（package.json 的 name） | dsh-odette-skin |
| patch entry id（cordis.patch.yml） | dsh-odette-skin |
| client 注册 id（lib/client.js 第 2 行） | dsh-odette-skin |

---

## 安装与验证

**安装方式**（本机已生效）：`dev_install_package(dir=C:\Users\24701\Desktop\skin\odette-skin, profile=desktop)`
→ dependencies 加 `link:` + bundles 加包名 + 建 node_modules junction；重启后由 bundles 正常装配。

**验证**：
1. 重启 DSH Desktop，UI 应变为 Odette 冰雪梦幻主题（深/浅双模式：背景图 + 主题 token + 毛玻璃）
2. 若无效，按顺序查：
   - patch entry id 与 client 注册 id 是否一致（本文件 Bug 2）
   - `dsh.client.inject` 里是否还残留已下线的包名（本文件 Bug 1）
   - `lib/index.js` 是否为空实现（本包设计如此：host 侧无运行时行为，全部逻辑在 client）
   - client bundle 是否被装配进 boot graph（宿主 loader 里应有 entry `dsh-odette-skin`）

**注意**：`lib/client.js` 有 4.4MB（素材以 base64 data URI 内嵌，由 `scripts/generate-art.cjs` 生成），首次加载会稍慢，属正常。